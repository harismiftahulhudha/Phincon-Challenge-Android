object Android {
    const val appId = "co.harismiftahulhudha.phinconchallenge"
    const val compileSdk = 33
    const val minSdk = 24
    const val targetSdk = 33
    private const val versionMajor = 1
    private const val versionMinor = 0
    private const val versionPatch = 0
    private const val versionBuild = 0
    const val versionCode = versionMajor * 10000 + versionMinor * 1000 + versionPatch * 100 + versionBuild
    const val versionName = "${versionMajor}.${versionMinor}.${versionPatch}"
    const val versionNameDev = "${versionMajor}.${versionMinor}.${versionPatch}.${versionBuild}"
}